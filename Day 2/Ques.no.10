#include <iostream.h>
using namespace std;
void main()
{
   int sum=0, a[10], i;
   cout<<"enter 10 element for an array ";
   for(i=0; i<10; i++)
     {
        cin>> a[i];
     }
    int max = a[0];
    for (i = 1; i < n; i++)
      {
         if (a[i] > max)
            max+= a[i];
      }
    cout<< max;
}
