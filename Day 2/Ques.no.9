#include<iostream.h>
using namespace std;
void main()
{
   int n, i, j, d;
   cout<< "enter the size of array";
   cin>> n;
   int a[n];
   cout<< "enter the value for array";
   for(i=0; i<n; i++)
     {
         cin>> a[i];
     }
   for(i=0; i<n-1; i++)
     {
         for(j=0; j<n; j++)
           {
              if((a[i]+a[j])==d)
              cout<< "true";
           }
     }
   cout<<"false";
}
