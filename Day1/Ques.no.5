#inculde<iostream.h>
using namespace std;
void outlierNumber(int arr[], int n)
{
int n, evencount=0 , oddcount = 0;
for(int i=0; i<n ; i++)
 {
     if( arr[i]%2==o)
     {
          evencount+= 1;
          int even = arr[i];
     }
     else
     {
          oddcount+=1;
          int odd = arr[i];
      }
 }
 if (evencount<oddcount)
      return even;
 else
      return odd;
}

void main()
{
int n, arr[];
cout<<"enter the size of array";
cin>> n;
cout<<" enter the element in array";
for (int i=0; i<n; i++)
{
cin>> arr[i];
}
outlierNumber(int arr[], int n);
}
}
