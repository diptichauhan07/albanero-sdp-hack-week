#include<iostream.h>
using namespace std;
void main()
 {
    int r, c, i, j;
    cout<<"enter the no of rows:"
    cin>>r;
    cout<<"enter the no of column:"
    cin>>c;
    int M[r][c];
    cout>>"enter the value of array:"
    for (i = 0; i < r; i++)
     {
        for (j = 0; j < c; j++)
         {
             cin>> mat[i][j];
         }
     }
    for (i = 0; i < r; i++)
     {
        for (j = 0; j < c; j++)
         {
             if(M[i][j]==0)
               {
                  M[i][j]=1;
               }
             else
               {
                 M[i][j]=0;
               }
         }
     }
   for (i = 0; i < r; i++)
    {
       for (j = 0; j < c; j++)
         {
            cout << M[i][j];
         }
        cout << endl;
    }
 }
