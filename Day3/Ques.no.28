#include<iostream.h>
using namespace std;
void main()
 {
    string inp;
    cout<<"enter the binary string";
    cin>> inp;
    cout<< inp;
    int count=0;
    for (int i = 0, length = str.length; i < length; i++) 
     {
        if (str[i] === '1') 
           count++;
     }
    if(count%2==0)
     {
        inp.append(0);
     }
    else
     {
        inp.append(1);
     }
    cout<<"parity:" <<inp <<endl;
 }
