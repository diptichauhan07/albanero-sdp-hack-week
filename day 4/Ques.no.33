#include<iostream.h>
using namespace std;
void main()
{   
    int i, n;
    cout<<"enter the size of an array:"
    cin>>n;
    string arr[n];
    cout<<"Enter element to an array : ";
    for(i=0; i<n; i++)
     {
       cin>>arr[i];
     }
    for(i=0;i<n;i++)
     {
        if(i==0)
         {
            if(islower(arr[i]))
            arr[i]=toupper(arr[i]);
         }
        else
         {
            if(arr[i]!=' ')
             {
                if(isupper(arr[i]))
                    a[i]=tolower(arr[i]);
             }
            else
             {
                i++;
                if(islower(arr[i]))
                    arr[i]=toupper(arr[i]);
             }
         }
     }
    cout<<"Now array is: ";
    for(i=0; i<n; i++)
     {
        cout<<arr[i];
     }
 
}
